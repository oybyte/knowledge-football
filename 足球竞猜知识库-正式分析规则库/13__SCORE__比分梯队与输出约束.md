---
schema_version: 1
document_id: DOC-SCORE-001
document_type: output_contract
title: 比分梯队与输出约束
domain_code: SCORE
retrieval_scope: active
track: formal
lifecycle: effective
version: 2.0.0
---

# 比分梯队与输出约束

## SCORE-001 输出顺序

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

输出顺序固定为：输入与截止时间、正式轨命中规则、正式轨分析过程、正式轨主结论、实验轨命中规则、实验轨独立结论、双轨一致或分歧、冲突与风险、比分梯队。

## SCORE-002 正式轨结论

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

核心结论必须引用新规则ID，并说明触发条件、关键反证和信号等级。最多输出两个明确方向，不得把三个市场同时写成强方向。结论不能表述为确定性保证。

## SCORE-003 极致平权比分

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

三项凯利最大差值不超过0.05时，核心与次选梯队覆盖主队一球小胜、平局和客队一球小胜；净胜两球以上只能放入边缘梯队。不得胜平负单选。

## SCORE-004 进球区间

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

总进球输出核心区间、容错区间和反向隐患。强信号可收窄，弱信号必须扩大容错；大小球方向与胜负方向保持独立。

## SCORE-005 实验轨独立输出

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

实验规则必须单列规则ID、触发条件、样本数/目标、实验观察和与正式轨的关系。固定标识为“实验轨，不纳入正式轨主结论”。实验轨与正式轨一致时不增加正式轨权重；冲突时必须提高风险等级。

## SCORE-006 缺失与无法裁决

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

缺少关键输入或冲突无法按适用范围解决时，回退通用规则、降低信号等级并明确说明，不强行给出单一方向。
