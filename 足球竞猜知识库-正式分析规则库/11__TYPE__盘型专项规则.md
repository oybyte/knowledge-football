---
schema_version: 1
document_id: DOC-TYPE-001
document_type: rule_module
title: 盘型专项规则
domain_code: TYPE
retrieval_scope: active
track: formal
lifecycle: effective
version: 2.0.0
---

# 盘型专项规则

## TYPE-001 交叉盘

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

交叉盘、关联盘和同型盘先完成结构识别，再判断主盘口。盘型关系只能作为辅助验证；形态相似不等于结论相同。

## TYPE-002 浅盘

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

浅盘优先检查基本面差异、主客场和热门属性。浅盘升档示强必须同时满足头部机构升档、升档后水位不高和凯利稳定或收紧，否则按弱信号处理。

## TYPE-003 平半与半球

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

平半低水、半球生死盘和浅盘共识小球等场景，必须检查凯利平权、热门冷热和赔付方向。浅盘信号的让球权重低于深盘明确共识。

## TYPE-004 中深盘

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

中深盘先判断合理档位和身价层级，再检查升降盘、超高水、退档和穿盘阻力。强队名气不能替代实力层级校验。

## TYPE-005 深盘

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

深盘统一检查是否存在碾压级实力、核心伤停、杯赛战意和强队脱敏条件。无基本面支撑的深盘诱上候选必须经过双向阻诱和欧亚联动验证。

## TYPE-006 盘型异常

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

盘口差达到两档以上、单机构偏离两档以上或出现极速跳盘时，标记异常盘。异常盘不得提高核心信号等级，只能作为风险和实验观察输入。
