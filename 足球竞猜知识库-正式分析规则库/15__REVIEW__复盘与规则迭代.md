---
schema_version: 1
document_id: DOC-REVIEW-001
document_type: governance
title: 复盘与规则迭代
domain_code: REVIEW
retrieval_scope: active
track: formal
lifecycle: effective
version: 2.0.0
---

# 复盘与规则迭代

## REVIEW-001 赛前视角

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

复盘先冻结赛前输入、截止时间、正式轨规则、实验轨规则和原始输出，再记录赛果、半场、关键事件和场面表现。不得先看赛果再重写赛前逻辑。

## REVIEW-002 偏差归因

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

偏差分为规则适配、执行错误、认知误判和随机事件四类。只有规则适配类问题可以提出新规则；执行错误先补入检查清单，随机事件不直接改规则。

## REVIEW-003 新规则进入实验轨

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

复盘发现的新规律必须先登记为实验轨。至少写明场景、输入、触发条件、阈值、动作、禁用条件、验证指标和样本目标。单场命中不能直接转正式轨。

## REVIEW-004 实验样本

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

默认同场景至少10场样本。平局规则要求相对基准提升至少12%，大小球规则至少提升10%；其他规则需预先定义指标。异常红牌直接影响、补时点球绝杀、重大乌龙或门将失误等样本要单独标注，不能任意剔除正常样本。

## REVIEW-005 退出机制

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

同场景达到8场仍较基准提升不足5%，或连续三场完全反向，可暂停或退休实验规则。暂停不影响正式轨正常运行。

## REVIEW-006 转正式轨

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

达到样本和指标门槛后，完成一次复盘审核和一次独立确认。转正保留规则ID，更新轨道和生命周期，记录样本、指标、审核人、版本和生效时间，并通过独立Git提交发布。

## REVIEW-007 正式规则失效评审

| 执行字段 | 统一要求 |
|---|---|
| inputs | 使用本规则主题对应的用户手动盘赔数据与联网背景字段；缺失时降级 |
| trigger | 仅在本节明确的场景与条件同时满足时触发 |
| thresholds | 以本节列出的数值、档位、时间节点和共识条件为准；临界值按保守侧处理 |
| action | 执行本节规定的判断、修正或风险标记 |
| output_effect | 只影响本主题对应的方向、区间、权重或风险，不跨域强行传导 |
| exceptions | 先检查本节边界、禁用条件、数据不足和冲突门禁 |
| relations | 由`18__REL__规则关系登记表.md`中的正式轨/实验轨关系约束 |

正式规则连续五场完全反向时启动失效评审。评审期可暂停强制触发；不得直接覆盖原规则，必须以新版本变更并保留记录。

## 复盘输出

每场记录规则触发次数、适用场次、方向命中、核心区间命中、比分命中、异常事件、实验轨样本进度和下一步动作。复盘只更新验证状态，不改写历史赛前记录。
